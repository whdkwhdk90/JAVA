A1)운영체제에 독립적, 사용하기 쉬움, 자동메모리관리, 네트워크와 분산환경 지원, 멀티쓰래드지원 

A2)public static void main(String[] args){ ... }

A3)컴퓨터가 나타내는 데이터의 저장 최소 단위. 2진수 값 하나를 저장할 수 있는 메모리 공간을 의미.

A4)메모리 공간에 한 개의 값을 기록하기 위한 장소(공간)

A5)boolean, char, byte, short, int, long, float, double
A6)8,16,8,16,32,64,32,64

A7)변수에 대입되는 값 자체를 의미한다.

A8)static, heap, stack

A9)java.util.Scanner

A10)(조건식)? 참일때 : 거짓일때 ;

A11)복합 대입 연산자

A12)if , switch

A13)for , do~while , while 

A14)for(초기식, 조건식, 증감식){ 수행될 문장; }

A15)break , continue , return

A16)같은 자료형의 변수를 하나의 묶음으로 다루는것.

A17)int[] iarr = new int[5];

A18)얕은 복사는 객체의 주소값을 가져와서 저장, 깊은 복사는 새로운 배열 객체를 생성하여 저장.
(얕은 복사는 값이 변하면 같이 변하고, 깊은복사는 값이 변해도 따로 변한다.)

A19)Academy[] arr = new Academy[3];

A20)현실 세계는 사물이나 개념처럼 독립되고 구분되는 각각의 객체로 이루어져 있으며, 발생하는 모든 사건들은 객체간의 상호작용이다. 이 개념을 컴퓨터로 옮겨 놓아 만들어낸 거이 객체지향언어이다.

A21)클래스에 정의된 내용대로 new 연산자를 통해 메모리 영역에 생성된 것.

A22)유연성을 확보하기 위해 구체적인 것은 제거한다는 의미. 프로그램에서 필요한 공통점을 추출하고, 불필요한 공통점을 제거하는 과정

A23)캡슐화, 상속, 다형성 , (추상화)

A24)클래스의 멤버변수에 대한 접근 권한은 private를 원칙. 클래스의 멤버 변수에 대한 연산처리를 목적으로 하는 함수들을 클래스 내부에 작성한다. 멤버함수는 클래스 밖에서 접근할 수 있도록 public으로 설정한다.

A25)서로 관련된 클래스 혹은 인터페이스의 묶음.

A26)패키지문과 클래스 선언 사이에 선언.

A27)프로그램 시작시 정적메모리 영역에 자동 할당되는 변수를 의미. 프로그램이 종료되면 사라진다.

A28)final

A29)객체가 new 연산자를 통해 heap 메모리 영역에 할당될때 객체 안에서 만들어지는 필드 초기화. 일종의 메소드로 전달된 초기값을 받아서 객체의 필드에 기록

A30)반환값이 없으며, 생성자명을 클래스명과 똑같이 지정해주어야함.

A31)객체 생성시 전달받은 값으로 객체를 초기화. JVM이 기본생성자를 자동으로 생성해주지않음. 상속에서 사용시 반드시 기본생성자 작성. 오버로딩을 이용하여 작성

A32)한 클래스 내에 동일한 이름의 메소드를 여러 개 작성하는 기법
A33)같은 메소드 이름, 다른 매개변수의 개수 또는 다른변수 타입, 순서.

A34)모든 인스턴스의 메소드에 숨겨진 채 존재하는 레퍼런스로, 할당된 객체를 가리킴. 함수 실행 시 전달되는 객체의 주소를 자동으로 받음.

A35)public, protected, default, private

A36)필드, 생성자, getter, setter 

A37)super

A38)자식 클래스가 상속 받은 부모 메소드를 재작성하는 것.
A39)메소드 이름 동일. 매개변수의 개수,타입동일. 리턴 타입 동일. private메소드 오버라이딩 불가

A40)instanceof

A41)컴파일 시 정적 바인딩된 메소드를 실행할 당시의 객체 타입을 기준으로 바인딩 되는것.

A42)[접근제한자] abstract class 클래스명 { } 
(선언문 안에 abstract를 포함시켜준다.)

A43)[접근제한자] interface 인터페이스명
(클래스 선언할때 class를 빼고 interface 를 포함시켜준다.
A44)모든 인터페이스의 메소드는 묵시적으로 public 이고 abstract.   변수는 묵시적으로 public static final. 객체생성은 안되나 참조형 변수로는 가능.

A45) 대소문자가 구분되며 길이 제한 없다/ 예약어를 사용하면 안된다./ 숫자로 시작하면 안된다./ 특수문자는 _와$만을 허용/ 여러단어이름은 단어의 첫글자를 대문자로.
















