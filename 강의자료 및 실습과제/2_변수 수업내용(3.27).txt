//단항연산자

//전위 연산은 연산 전에 먼저 계산
//a =10, b= 20;
//a + (++b) = 31, a=10, b=21;
//a + (b++) = 30, a=10, b=21;
		
//논리연산자는 a&&b일때 a부터 계산하기에
 a가 false이면 b는 값을 계산안해서 추정 불가능.
		
//삼항 연산자 : int result1 = a > b ? a++ : b--; 이면
//a > b 라는 조건에서 true면(a>b이면) a++가 result1로 들어가고
//a > b가 false면 b--가 result1로 들어간다.

******문제가 반드시 나온다*******