function makeTable(elem){
	var $table = $("<table border=1>");
	
	//변수 선언은 위처럼 할 수 있고
	//지금처럼도 할 수 있음.
	for(var i=0; i<1; i++){
		$tr = $("<tr>");	
		for(var j=0;j<elem.eq(0).children().length;i++){
			$td = $("<td>").append( elem.eq(0).children().eq(j).prop("tagName"));
		}
	}
}